using Xunit;
using System.Threading.Tasks;
using ESL.CO.React.Controllers;
using ESL.CO.React.Models;
using Microsoft.AspNetCore.Mvc;
using Microsoft.Extensions.Caching.Memory;

namespace ESL.CO.Tests
{
    public class SampleDataControllerTests
    {
        //[Fact]
        //public async Task Create_ReturnsBadRequest_GivenInvalidModel()
        //{
        //    // Arrange & Act
        //    var id = 620;
        //    //var mockRepo = new Mock<IBrainstormSessionRepository>();
        //    IMemoryCache cache = default(IMemoryCache);
        //    var controller = new SampleDataController(cache);
        //    controller.ModelState.AddModelError("error", "some error");

        //    // Act
        //    var result = await controller.BoardData(id);

        //    // Assert
        //    Assert.IsType<BadRequestObjectResult>(result);
        //}
    }
}
